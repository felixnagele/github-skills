import ResourceView from "./ResourceView";

function Teams() {
  return (
    <ResourceView
      resourceKey="teams"
      title="Teams"
      emptyMessage="No teams found."
    />
  );
}

export default Teams;
